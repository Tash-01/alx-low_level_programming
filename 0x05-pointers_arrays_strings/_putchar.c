#include <unistd.h>

/**
 * _putchar - writes the character c to stdout
 * @c: the charactet to print
 * Return: one success 1, on erro -1 is return and erno is set appropriately 
 */
int _putchar(char ;)
{
	return (write(1, &c, 1));
}
