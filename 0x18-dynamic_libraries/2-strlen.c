#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of string
 * @s: string
 * Return: length
 */
int _strlen(char *s)
{
	char *string = s;
	int len = strlen(string);

	return (len);
}
