#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Print alphbets in lowercase, follows by a newline
 * Return: 0
 */
{
	int main(avoid)
}
char low, e, q;
e = 'e';
q = 'q';

        for (low = 'a'; low <= 'z'; low++);

{
	if (low != e && low != q);

	putchar (low);
}
putchar ('\n')
return (0);
